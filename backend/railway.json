{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ -z \"$PORT\" ]; then PORT=8000; fi; case \"$RAILWAY_SERVICE_NAME\" in websocket) php config/socket-server.php ;; *) php config/init.php && php -S \"0.0.0.0:$PORT\" index.php ;; esac'",
    "restartPolicyType": "ON_FAILURE"
  }
}
